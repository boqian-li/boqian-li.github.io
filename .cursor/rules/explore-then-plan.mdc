---
description: 当用户提出模糊的代码构建或修改请求时，先探索代码库再规划，最后才动手写代码
alwaysApply: true
---

# 探索优先，规划先行

当用户提出**模糊或范围较大**的代码构建/修改请求时，遵循以下流程：

## 判断触发条件

如果请求满足以下任一条件，视为"模糊请求"：
- 未指定具体文件或函数
- 涉及多个模块或跨文件改动
- 需求描述偏高层、缺少实现细节
- 可能有多种实现路径

## 工作流程

1. **询问确认**：先向用户确认是否需要"构建前先探索，写代码前先规划"
2. **全面探索**：用户确认后，使用 Task (explore subagent) 等工具自主深入探索代码库，理解项目结构、相关模块、功能和依赖关系
3. **切换 Plan 模式**：探索完成后，切换到 Plan 模式，输出清晰的实现规划，包括：
   - 需要修改/新建的文件列表
   - 每个文件的改动要点
   - 改动顺序和依赖关系
4. **执行实现**：用户确认规划后，再切换回 Agent 模式开始编写代码

## 例外情况

以下情况可以跳过此流程，直接执行：
- 用户明确指定了要修改的文件和具体改法
- 单行或少量行的简单修改（如改个变量名、修个 typo）
- 用户明确表示"直接改"或"不需要规划"
- 用户并没有在构建代码或修改代码
